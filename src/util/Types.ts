export default interface Weather {
    id: string,
    temp: string,
    temp_min: string,
    temp_max: string,
    main: string,
    name: string,
    speed: string,
    country: string,
    sunset: string
}
